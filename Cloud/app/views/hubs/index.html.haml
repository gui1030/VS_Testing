.panel.panel-default.no-border
  .panel-heading
    %h1= link_to @unit.name, @unit
    %h2
      VeriConnect
      - if policy(Hub).new?
        = link_to '+', new_unit_hub_path(@unit)
  %ul.list-group= render partial: 'hub', collection: @hubs
