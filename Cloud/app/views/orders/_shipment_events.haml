.panel.panel-default
  %ul.list-group
    - @tracking_info.shipment_events.each do |event|
      %li.list-group-item #{event.name} at #{event.location.city}, #{event.location.state} on #{event.time.in_time_zone.strftime('%Y-%m-%d %I:%M %p')}.
